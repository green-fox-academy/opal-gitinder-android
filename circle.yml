general:
    build_dir: GiTinder/app/

general:
    artifacts:
        - GiTinder/app/build/outputs/apk/

dependencies:
    override:
        - ./gradlew dependencies

test:
    override:
        - ./gradlew :app:test

